{
  "name": "agentic-postman",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "lint": "eslint server.js app.js workspace-utils.js test/**/*.test.js",
    "ci": "npm run lint && npm run test && npm audit --audit-level=high"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
